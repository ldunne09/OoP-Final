<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_lab_oop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.Create_propfile"></activity>
        <activity android:name=".views.Themes" />
        <activity android:name=".views.LoadEntry_activity" />
        <activity android:name=".views.LoadFile_Activity" />
        <activity android:name=".views.UserMenuActivity" />
        <activity android:name=".views.PasswordEntryActivity" />
        <activity android:name=".views.MainMenuActivity" />
        <activity android:name=".views.CreateEntryActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>